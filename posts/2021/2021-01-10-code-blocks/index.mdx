---
title: Code Blocks
date: 2021-01-10 10:20:00
published: true
cover: './cover.jpg'
coverCredit: "Patrick Robert Doyle @unsplash"
coverLink: "https://unsplash.com/photos/_8bM_EqmFgM"
---

## Yes! Some code!

Here is the `Dump` component!

```jsx 
const Dump = props => (
  <div
    style={{
      fontSize: 20,
      border: '1px solid #efefef',
      padding: 10,
      background: 'white',
    }}>
    {Object.entries(props).map(([key, val]) => (
      <pre key={key}>
        <strong style={{ color: 'white', background: 'red' }}>
          {key} ?
        </strong>
        {JSON.stringify(val, '', ' ')}
      </pre>
    ))}
  </div>
);

render(<Dump props={['One', 'Two', 'Three', 'Four']} />);
```